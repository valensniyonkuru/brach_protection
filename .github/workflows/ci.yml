name: CI

on:
  pull_request:
    branches: [dev, stage, main]
  push:
    branches: [dev, stage, main]

permissions:
  contents: read

jobs:
  frontend-build-test:
    name: frontend-build-test
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.detect.outputs.should_run }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect frontend project
        id: detect
        shell: bash
        run: |
          if [ -f "frontend/package.json" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Node (with cache)
        if: steps.detect.outputs.should_run == 'true' && hashFiles('frontend/package-lock.json') != ''
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Setup Node (no cache)
        if: steps.detect.outputs.should_run == 'true' && hashFiles('frontend/package-lock.json') == ''
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install frontend dependencies
        if: steps.detect.outputs.should_run == 'true'
        working-directory: frontend
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Run frontend tests
        if: steps.detect.outputs.should_run == 'true'
        working-directory: frontend
        run: npm test -- --watch=false

      - name: Build frontend
        if: steps.detect.outputs.should_run == 'true'
        working-directory: frontend
        run: npm run build

      - name: Frontend not present (no-op)
        if: steps.detect.outputs.should_run != 'true'
        run: echo "frontend/package.json not found; skipping frontend job."

  backend-build-test:
    name: backend-build-test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        if: hashFiles('backend/pom.xml', 'backend/build.gradle', 'backend/build.gradle.kts', 'backend/gradlew', 'backend/mvnw') != ''
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Build and test backend
        shell: bash
        run: |
          if [ ! -d "backend" ]; then
            echo "backend/ directory not found; skipping backend job."
            exit 0
          fi

          cd backend
          if [ -x "./mvnw" ]; then
            ./mvnw -B test
          elif [ -f "pom.xml" ]; then
            mvn -B test
          elif [ -x "./gradlew" ]; then
            ./gradlew test
          elif [ -f "build.gradle" ] || [ -f "build.gradle.kts" ]; then
            gradle test
          else
            echo "No Maven/Gradle project files found in backend/; skipping backend job."
            exit 0
          fi
